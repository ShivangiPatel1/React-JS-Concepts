// React will assign the current property with the DOM element when the component mounts, 
// and assign it back to null when it unmounts. ref updates happen before componentDidMount 
// or componentDidUpdate lifecycle methods.

